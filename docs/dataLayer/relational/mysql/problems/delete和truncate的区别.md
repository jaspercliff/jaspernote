# delete和truncate的区别.md

truncate 一次清理完表 比delete快的多

## delete from 
它属于 DML 操作，会老老实实地把 300 万条数据一行一行地删除，并且把每一行的删除操作都记录到 Undo Log 和 Binlog 日志里。
这会导致你的数据库疯狂刷盘，CPU 飙高，甚至可能把磁盘空间撑爆。
关键点：它不会重置自增 ID

## truncate 
它不会一条条去删数据，而是直接把存放这张表的数据文件“推倒重建”，释放磁盘空间。
关键点：它会把主键自增 ID 重新重置为 1
