# sql 优化 

## 索引 


- 给where, group by,join,order by 加索引
- 建立联合索引时，查询条件必须从最左边开始(mysql 优化器会优化)，不要缺失最左边,遇到范围查询或者缺失某一列会发生部分匹配
- 尽量使用覆盖索引（让索引的叶子节点包含所要的数据），避免回表
- 尽量避免[索引失效](./invalid.md)
- 加行锁时，where条件一定要命中索引，不然行锁会直接退化成表锁(行锁是基于索引加的)

## other
- 一些子查询可以用窗口函数替换(相当于双层循环 改为一次循环)
