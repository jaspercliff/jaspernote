# plantuml

```zsh
npm install markdown-it-textual-uml
```

```zsh
export default defineConfig({
    markdown: {
        config: (md) => {
            md.use(markdownItTextualUml);
        }
    }
})
```
